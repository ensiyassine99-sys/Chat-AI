name: Build and Deploy Chatbot Application

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

env:
  DOCKER_REGISTRY: ghcr.io
  IMAGE_PREFIX: ensiyassine99-sys
  BACKEND_IMAGE: chatbot-backend
  FRONTEND_IMAGE: chatbot-frontend

jobs:
  # ==================== JOB 1: BUILD ====================
  build:
    name: Build Docker Images
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.DOCKER_REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata for Backend
        id: meta-backend
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.DOCKER_REGISTRY }}/${{ env.IMAGE_PREFIX }}/${{ env.BACKEND_IMAGE }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Extract metadata for Frontend
        id: meta-frontend
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.DOCKER_REGISTRY }}/${{ env.IMAGE_PREFIX }}/${{ env.FRONTEND_IMAGE }}
          tags: |
            type=ref,event=branch
            type=ref,event=pr
            type=sha,prefix={{branch}}-
            type=raw,value=latest,enable={{is_default_branch}}

      - name: Build and push Backend image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/Dockerfile.backend
          push: true
          tags: ${{ steps.meta-backend.outputs.tags }}
          labels: ${{ steps.meta-backend.outputs.labels }}
          cache-from: type=registry,ref=${{ env.DOCKER_REGISTRY }}/${{ env.IMAGE_PREFIX }}/${{ env.BACKEND_IMAGE }}:buildcache
          cache-to: type=registry,ref=${{ env.DOCKER_REGISTRY }}/${{ env.IMAGE_PREFIX }}/${{ env.BACKEND_IMAGE }}:buildcache,mode=max

      - name: Build and push Frontend image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./docker/Dockerfile.frontend
          push: true
          tags: ${{ steps.meta-frontend.outputs.tags }}
          labels: ${{ steps.meta-frontend.outputs.labels }}
          cache-from: type=registry,ref=${{ env.DOCKER_REGISTRY }}/${{ env.IMAGE_PREFIX }}/${{ env.FRONTEND_IMAGE }}:buildcache
          cache-to: type=registry,ref=${{ env.DOCKER_REGISTRY }}/${{ env.IMAGE_PREFIX }}/${{ env.FRONTEND_IMAGE }}:buildcache,mode=max

      - name: Image digest
        run: |
          echo "Backend: ${{ steps.meta-backend.outputs.tags }}"
          echo "Frontend: ${{ steps.meta-frontend.outputs.tags }}"

  # ==================== JOB 2: DEPLOY ====================
  deploy:
    name: Deploy to Production Server
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Install SSH key
        uses: webfactory/ssh-agent@v0.8.0
        with:
          ssh-private-key: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Add server to known hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ secrets.SERVER_IP }} >> ~/.ssh/known_hosts

      - name: Prepare environment on server
        run: |
          ssh ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_IP }} "
          echo 'Préparation de l environnement...' &&
          mkdir -p ~/chatbot-app &&
          
          # Supprimer l'ancien fichier docker-compose
          if [ -f ~/chatbot-app/docker-compose.yml ]; then
            rm -f ~/chatbot-app/docker-compose.yml;
            echo '- docker-compose.yml supprimé';
          fi &&
          
          # Réinitialiser le répertoire docker
          rm -rf ~/chatbot-app/docker &&
          mkdir -p ~/chatbot-app/docker &&
          echo '- Répertoire docker réinitialisé' &&
          
          # Créer le répertoire backend pour .env
          rm -rf ~/chatbot-app/backend &&
          mkdir -p ~/chatbot-app/backend &&
          echo '- Répertoire backend créé' &&
          
          # Créer les répertoires pour les volumes
          mkdir -p ~/chatbot-app/database &&
          mkdir -p ~/chatbot-app/uploads &&
          mkdir -p ~/chatbot-app/logs &&
          echo '- Répertoires de données créés' &&
          
          echo 'Environnement préparé avec succès!'
          "

      - name: Copy configuration files to server
        run: |
          scp docker-compose.prod.yml ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_IP }}:~/chatbot-app/docker-compose.yml
          scp docker/nginx.conf ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_IP }}:~/chatbot-app/docker/nginx.conf
          scp backend/.env ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_IP }}:~/chatbot-app/backend/.env

      - name: Stop and remove existing containers
        run: |
          ssh ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_IP }} "
          echo 'Gestion des conteneurs existants...' &&
          
          # Identifier les conteneurs de l'application par leurs noms
          APP_CONTAINERS=\$(docker ps -a --format '{{.Names}}' | grep -E 'chatbot-redis|chatbot-backend|chatbot-frontend|chatbot-nginx' || true) &&
          
          if [ -n \"\$APP_CONTAINERS\" ]; then
            echo 'Conteneurs existants trouvés. Arrêt et suppression...' &&
            
            # Arrêter d'abord tous les conteneurs identifiés
            for CONTAINER in \$APP_CONTAINERS; do
              echo \"- Arrêt de \$CONTAINER...\" &&
              docker stop \$CONTAINER || true
            done &&
            
            # Puis les supprimer
            for CONTAINER in \$APP_CONTAINERS; do
              echo \"- Suppression de \$CONTAINER...\" &&
              docker rm \$CONTAINER || true
            done
          else
            echo 'Aucun conteneur de l application trouvé. Premier déploiement.'
          fi &&
          
          # Nettoyage des conteneurs arrêtés
          echo 'Nettoyage final des conteneurs inutilisés...' &&
          docker container prune -f
          "

      - name: Login to registry and deploy services
        run: |
          ssh ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_IP }} "
          cd ~/chatbot-app &&
          echo 'Connexion au registre Docker...' &&
          echo '${{ secrets.GITHUB_TOKEN }}' | docker login -u '${{ github.actor }}' --password-stdin '${{ env.DOCKER_REGISTRY }}' &&
          
          # Définition du nom du projet
          export COMPOSE_PROJECT_NAME=chatbot-app &&
          
          # Pull des images
          echo 'Téléchargement des images...' &&
          docker-compose -f docker-compose.yml pull &&
          
          # Démarrage des services
          echo 'Démarrage des services...' &&
          docker-compose -f docker-compose.yml up -d
          "

      - name: Verify deployment and cleanup
        run: |
          ssh ${{ secrets.SERVER_USER }}@${{ secrets.SERVER_IP }} "
          echo 'Vérification des conteneurs...' &&
          sleep 10 &&
          docker ps &&
          
          # Nettoyage des images Docker inutilisées
          echo 'Nettoyage des images inutilisées...' &&
          docker image prune -f &&
          
          echo 'Déploiement terminé avec succès!'
          "

  # ==================== JOB 3: NOTIFICATION ====================
  notify:
    name: Deployment Status
    runs-on: ubuntu-latest
    needs: [build, deploy]
    if: always()
    
    steps:
      - name: Check deployment result
        run: |
          if [ "${{ needs.deploy.result }}" == "success" ]; then
            echo "🚀 Déploiement réussi!"
            echo "✅ Backend: ghcr.io/ensiyassine99-sys/chatbot-backend:latest"
            echo "✅ Frontend: ghcr.io/ensiyassine99-sys/chatbot-frontend:latest"
          else
            echo "❌ Échec du déploiement"
            exit 1
          fi